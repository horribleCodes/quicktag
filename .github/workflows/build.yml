name: Build

on:
  push:
    branches: [main, master, release]
  pull_request:
    branches: [main, master, release]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest -m "not integration"

  build-windows:
    runs-on: windows-latest
    needs: test

    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Restore venv cache
        uses: actions/cache@v4
        with:
          path: .venv
          key: venv-${{ runner.os }}-py311-${{ hashFiles('pyproject.toml') }}-onnx-v1

      - name: Restore PyInstaller build cache
        uses: actions/cache@v4
        with:
          path: build/quicktag
          key: pyinstaller-${{ runner.os }}-${{ hashFiles('quicktag.spec', 'hooks/**', 'src/quicktag/**') }}-onnx-v1

      - name: Restore ExifTool cache
        uses: actions/cache@v4
        with:
          path: assets/exiftool
          key: exiftool-13.59-v1

      - name: Cache ONNX model export
        uses: actions/cache@v4
        with:
          path: smoke-root/.cache/huggingface/onnx-export
          key: onnx-model-google-siglip2-base-patch16-224-v1

      - name: Build QuickTag
        shell: pwsh
        run: ./build.ps1

      - name: Prepare ONNX smoke model
        shell: pwsh
        run: |
          $exportDir = Join-Path $env:GITHUB_WORKSPACE "smoke-root/.cache/huggingface/onnx-export/google--siglip2-base-patch16-224"
          if (-not (Test-Path (Join-Path $exportDir "model.onnx"))) {
            .\.venv\Scripts\python.exe scripts/export_onnx_model.py --output $exportDir
          }

      - name: Smoke test frozen binary
        shell: pwsh
        run: |
          $smoke = Join-Path $env:GITHUB_WORKSPACE "smoke-root"
          New-Item -ItemType Directory -Force -Path (Join-Path $smoke "input") | Out-Null
          New-Item -ItemType Directory -Force -Path (Join-Path $smoke "output") | Out-Null
          Copy-Item config.example.yaml (Join-Path $smoke "config.yaml") -Force
          Copy-Item tags.example.yaml (Join-Path $smoke "tags.yaml") -Force
          Copy-Item tests/fixtures/tiny.png (Join-Path $smoke "input/tiny.png") -Force
          $exe = Join-Path $env:GITHUB_WORKSPACE "dist/win/quicktag/quicktag.exe"
          & $exe --help
          & $exe --root $smoke
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (-not (Test-Path (Join-Path $smoke "output/tiny.png"))) {
            Write-Error "Expected tagged output image was not created"
            exit 1
          }

      - name: Report artifact size
        shell: pwsh
        run: |
          $dist = Join-Path $env:GITHUB_WORKSPACE "dist/win/quicktag"
          $size = (Get-ChildItem -Path $dist -Recurse -File | Measure-Object -Property Length -Sum).Sum
          Write-Host "Windows dist size bytes: $size"

      - name: Upload artifact
        if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main')
        uses: actions/upload-artifact@v4
        with:
          name: quicktag-windows
          path: dist/win/quicktag/
          retention-days: 90

  build-linux:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Restore venv cache
        uses: actions/cache@v4
        with:
          path: .venv
          key: venv-${{ runner.os }}-py311-${{ hashFiles('pyproject.toml') }}-onnx-v1

      - name: Restore PyInstaller build cache
        uses: actions/cache@v4
        with:
          path: build/quicktag
          key: pyinstaller-${{ runner.os }}-${{ hashFiles('quicktag.spec', 'hooks/**', 'src/quicktag/**') }}-onnx-v1

      - name: Cache ONNX model export
        uses: actions/cache@v4
        with:
          path: smoke-root/.cache/huggingface/onnx-export
          key: onnx-model-google-siglip2-base-patch16-224-v1

      - name: Install ExifTool
        run: sudo apt-get update && sudo apt-get install -y libimage-exiftool-perl

      - name: Build QuickTag
        run: chmod +x build.sh && ./build.sh

      - name: Prepare ONNX smoke model
        run: |
          export_dir="smoke-root/.cache/huggingface/onnx-export/google--siglip2-base-patch16-224"
          if [ ! -f "$export_dir/model.onnx" ]; then
            .venv/bin/python scripts/export_onnx_model.py --output "$export_dir"
          fi

      - name: Smoke test frozen binary
        run: |
          smoke="$GITHUB_WORKSPACE/smoke-root"
          mkdir -p "$smoke/input" "$smoke/output"
          cp config.example.yaml "$smoke/config.yaml"
          cp tags.example.yaml "$smoke/tags.yaml"
          cp tests/fixtures/tiny.png "$smoke/input/tiny.png"
          ./dist/linux/quicktag/quicktag --help
          ./dist/linux/quicktag/quicktag --root "$smoke"
          test -f "$smoke/output/tiny.png"

      - name: Report artifact size
        run: du -sh dist/linux/quicktag

      - name: Upload artifact
        if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main')
        uses: actions/upload-artifact@v4
        with:
          name: quicktag-linux
          path: dist/linux/quicktag/
          retention-days: 90
