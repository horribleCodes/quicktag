Place the Windows ExifTool distribution here before building:

- exiftool.exe
- exiftool_files/   (required support files from the official Windows zip)

The Windows build script (`build.ps1`) downloads these automatically.
For local development on Linux/macOS, install ExifTool via your package manager
and ensure `exiftool` is on PATH.
